# URL do projeto Supabase
SUPABASE_URL=

# Chave anônima (pública) do Supabase — para operações restritas por RLS
SUPABASE_ANON_KEY=

# Chave de service role (privada, NUNCA expor no frontend) — só se o backend
# precisar de acesso administrativo ao banco
SUPABASE_SERVICE_ROLE_KEY=
